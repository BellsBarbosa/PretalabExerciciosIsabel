const base= 5;
const altura =  8;

const area = base * altura;
const perimetro = 2 * (base + altura);

console.log (`A area do retangulo de base ${base} e altura ${altura} corresponde a ${area} e perimetro ${perimetro}`);